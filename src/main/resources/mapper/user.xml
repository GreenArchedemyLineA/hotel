<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dodam.hotel.repository.interfaces.UserRepository">
	<select id="findUserByLoginFormDto" resultType="com.dodam.hotel.dto.UserResponseDto$LoginResponseDto">
		select *
		from user_tb
		where email = #{email} and password = #{password}
	</select>
	
	<update id="updateUserByEmail">
		update user_tb
		set password = #{password}, tel = #{tel}
		where email = #{email}
	</update>
	
	<select id="findUserByEmail" resultType="com.dodam.hotel.repository.model.User">
		select *
		from user_tb
		where email = #{email}
	</select>
	
	<insert id="insert">
	INSERT INTO user_tb (email, password, name, gender, birth, tel)
	VALUES (#{email}, #{password}, #{name}, #{gender}, #{birth}, #{tel})
</insert>
</mapper>
