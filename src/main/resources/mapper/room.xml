<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dodam.hotel.repository.interfaces.RoomRepository">
	<select id="findAllRoom" resultType="com.dodam.hotel.repository.model.Room">
		<if test="type=='All'">
			select *
			from room_tb
			group by name
		</if>
		<if test="type=='디럭스'">
			select *
			from room_tb
			where name like #{name}
			group by name
		</if>
		<if test="type=='프리미엄'">
			select *
			from room_tb
			where name like #{name}
			group by name
		</if>
		<if test="type=='스위트'">
			select *
			from room_tb
			where name like #{name}
			group by name
		</if>
	</select>
	
	<select id="findRoomById" resultType="com.dodam.hotel.repository.model.Room">
		select *
		from room_tb
		where id = #{id}
	</select>
	
</mapper>
