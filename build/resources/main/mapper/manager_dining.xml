<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- writer: 이현서 -->
<mapper namespace="com.dodam.hotel.repository.interfaces.DiningRepository">
    <select id="findAllDining" resultType="com.dodam.hotel.repository.model.managertest.Dining">
        select * from dining_tb;
    </select>
<!--
        insert into
            dining_tb(name, description, location, availability, status_desc, image)
               values ("test", "test", "test", true, "test", null)
-->

<!--    create table pay_tb(-->
<!--        id int primary key, auto_increment,-->
<!--        price int not null,-->
<!--        reservation_id int not null,-->
<!--        foreign-->
</mapper>
